/* fichier carte.h */

/**
* \struct ceci est une declaration d'un type abstrait
* carte, lors de l'implementation, il faudra specifier
* les differents champs du type carte 
* (une carte sera donc caracterisee par un tuple)
*/
typedef struct carte_base *carte;
